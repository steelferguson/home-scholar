// Defaults for quiz-game lessons whose content JSON omits the field.
// Kept dependency-free so the dev preview can import them without pulling
// in the Supabase client.
export const DEFAULT_COIN_PER_CORRECT = 2
export const DEFAULT_ARCADE_COST = 10
export const DEFAULT_ARCADE_SECONDS = 180
